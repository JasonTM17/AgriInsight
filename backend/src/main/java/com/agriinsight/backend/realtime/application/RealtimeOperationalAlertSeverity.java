package com.agriinsight.backend.realtime.application;

/** Severity values persisted by the metadata-only operational alert projection. */
public enum RealtimeOperationalAlertSeverity {
    WARNING,
    CRITICAL
}
